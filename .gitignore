# Large USB base images — not needed in the repo, too big for GitHub
# (distribute via releases / external storage if ever needed)
setup_library/files/usb_base/
setup_library/files/**/usb[0-9]*

# Editor / OS junk
*.swp
.DS_Store

# Generated Rasbase archive - too large for GitHub
setup_library/files/containers/rasbase_parts/rasbase.tar
